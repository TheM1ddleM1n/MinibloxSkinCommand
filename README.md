# MinibloxSkinCommand
this is the miniblox skin command for (F12 - console). Please use responsibly.


First put `token.js` into console first to get the token.

then use `skin.js` and replace the `your_token_goes_here` with ur token!

enjoy!
